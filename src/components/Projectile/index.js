// spawn projectile
/**
 * start position
 * end position
 * move speed
 * sprite 
 * 
 * onDestroy
 */

const Projectile = ({
  startPos,
  direction,
  maxDistance,
  speed,
  spriteURL,
  whileAlive, // function to repeat while alive?
  onDestroy, // if we should do anything once it breaks?
}) => {



}